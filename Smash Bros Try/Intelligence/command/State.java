package command;

public class State {

	/*
	 * The point of a state is to make sure player AI isn't based off button inputs to a series of individual actions
	 * But rather its off button inputs to a function that takes over the AI of the minion its effecting
	 * 
	 */
	
	
	
	
	
	
}
